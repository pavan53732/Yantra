# Planner Protocol

This protocol defines how planning inputs, outputs, and handoffs are represented so planning remains reviewable and scope-aware.

## Purpose

The planner protocol keeps mission planning structured, visible, and aligned with approval and execution steps.

## Expectations

- plan inputs are explicit,
- assumptions are visible,
- outputs are reviewable,
- changes in scope remain traceable,
- planner behavior supports verification and approval gates.
