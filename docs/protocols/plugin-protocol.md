# Plugin Protocol

This protocol defines how plugin interactions remain explicit, reviewable, and safe inside the Yantra product model.

## Purpose

Plugins extend Yantra only through controlled boundaries that preserve product, security, and governance integrity.

## Expectations

- plugin contracts are explicit,
- capabilities exposed by plugins are reviewable,
- failures are bounded,
- plugin behavior remains compatible with mission workflows.
