# Provider Protocol

The provider protocol defines how Yantra communicates with external providers while preserving stable internal workflow behavior.

## Expectations

- request and response handling remain explicit,
- failures are visible,
- provider-specific behavior does not become hidden product policy,
- protocol behavior supports traceability.
