# IPC Architecture

Yantra's Electron IPC architecture should remain secure, minimal, and explicit. Renderer access to privileged desktop capabilities should flow through audited IPC contracts rather than through direct unrestricted process access.[cite:18]
