# Security Model

Yantra should remain human-supervised by default, with explicit approval boundaries around powerful actions such as file modification, terminal execution, package installation, Git operations, and deployment. Windows-first, local-first trust boundaries should guide early product decisions.[cite:18][cite:21][cite:27]
