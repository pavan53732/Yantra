# Provider Model

Yantra should expose an AI platform layer rather than a simple provider list. Providers should be abstracted behind a stable product model that supports routing, tool use, streaming, cost visibility, and future role-based model selection.[cite:18]
