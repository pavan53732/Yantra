# Product Architecture

Yantra should be understood as a Windows desktop product composed of a workspace layer, mission layer, agent layer, knowledge layer, capability layer, and verification layer, all presented through a minimal mission-oriented product shell.[cite:18]
