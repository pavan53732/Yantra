# API Documentation

Public API contracts exposed by the Yantra engine (`packages/*`) to any
client — desktop, CLI, headless server, CI/CD, or IDE integration.

## Contents

*API references will be added here as `packages/*` public interfaces are
specified and stabilized in Phase 2/3. Each package's public API must be
documented here before other clients depend on it.*
