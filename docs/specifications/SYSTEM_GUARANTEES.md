# System Guarantees

Yantra guarantees:

- Every Mission has exactly one owner.
- Every Task belongs to exactly one Mission.
- Every state transition is logged.
- Every agent decision is attributable.
- Every Artifact has provenance.
- Every plugin is isolated.
- Every protocol is versioned.
- Every capability is replaceable.
