# System Invariants

System invariants are repository truths that Yantra should preserve across planning, implementation, verification, and release.

## Invariants

- The PRD remains the source of truth.
- Mission scope must remain explicit.
- Approval and verification gates must not be bypassed silently.
- Repository changes must remain reviewable and attributable.
- Product behavior should remain aligned with Windows-first, mission-first direction.
