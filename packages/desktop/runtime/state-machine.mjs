const sessionTransitions={idle:['booting','shutdown'],booting:['ready','error'],ready:['running-mission','shutdown','error'],'running-mission':['ready','error','shutdown'],error:['shutdown'],shutdown:[]}; const windowTransitions={created:['ready','closed'],ready:['visible','closed'],visible:['closed'],closed:[]}; export function transitionSession(from,to){ if(!sessionTransitions[from].includes(to)) throw new Error(`Invalid desktop session transition: ${from} -> ${to}`); return to; } export function transitionWindow(from,to){ if(!windowTransitions[from].includes(to)) throw new Error(`Invalid desktop window transition: ${from} -> ${to}`); return to; }
